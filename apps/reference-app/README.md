# Reference App

The Reference App is a fixed, static dashboard composition. Its only state is
the selected registered design system; the same JSX tree is rendered for either
package so visual differences remain attributable to the system.

Run it from the workspace with `pnpm --filter @prism-system/reference-app dev`.
