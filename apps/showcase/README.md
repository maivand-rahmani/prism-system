# Showcase

The Showcase is the V1 component laboratory. It registers both public design
system packages once, then resolves the selected registry entry for the same
foundation and component composition. It intentionally contains no product
logic.

Run it from the workspace with `pnpm --filter @prism-system/showcase dev`.
